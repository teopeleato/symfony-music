{% extends 'base.html.twig' %}

{% block title %}New Band{% endblock %}

{% block body %}
    <h1>Create new Band</h1>

    {{ include('band/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('band_index') }}">back to list</a>
{% endblock %}
